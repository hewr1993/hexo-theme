<!DOCTYPE html>
<html>
<%- partial('casper/head') %>
<!--<% if (typeof page.title == 'undefined') { %>
<body class="home-template">
<% } else { %>
<body class="post-template">
<% } %>-->
<body class="home-template" style="background:url(/img/img-bg.jpg) fixed; background-size: 100% 100%;">
<div id="perspective" class="perspective effect-movedown">
  <div class="container">
    <!-- wrapper -->
    <div class="wrapper">

      <%- partial('casper/header') %>
	  <div class="surface-panel">
      <%- body %>
      <%- partial('casper/footer') %>
	  </div>
      <%- partial('casper/after_all') %>

  </div>
</div>

<nav  class="outer-nav top horizontal">
<% for (var i in theme.menu){ %>
          <a class="<%- theme.navclass[i]||'default' %>"  href="<%- url_for(theme.menu[i]) %>"><span><%= i %></span></a>
<% } %>
</nav>

</div>
</body>
</html>
